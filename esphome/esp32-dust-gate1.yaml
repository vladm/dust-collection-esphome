# =====================================================================
# esp32-dust-gate1.yaml — Blast gate 1 (ESP32-C6 board)
# TEMPLATE: adjust board id, pins, and servo trim to this node's
# hardware before flashing (values below are from the old esp32c6-*
# configs — verify against the actual wiring).
# =====================================================================
substitutions:
  device_name: esp32-dust-gate1
  line_channel: line1_current
#  # C6 board + pin overrides (package defaults are for the XIAO C5):
#  board: esp32-c6-devkitc-1
#  servo_pin: GPIO3
#  led_opened_pin: GPIO10
#  led_closed_pin: GPIO1
  # Servo trim — calibrate per rig:
  sweep_step: "0.047"
  servo_direction: "1.0"
  level_min: "5%"
  level_max: "10.6%"

packages:
  dust_gate: !include dust-gate-common.yaml

wifi:
  band_mode: 2.4GHZ
